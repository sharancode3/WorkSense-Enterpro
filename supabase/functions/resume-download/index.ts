// GENERATED by scripts/bundle-functions.mjs — do not edit by hand.
// Edit supabase/functions/_shared/* and <fn>/source.ts, then re-run the bundler.

import { createClient } from "https://esm.sh/@supabase/supabase-js@2.39.0";

const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
};
const json = (body: unknown, status = 200) =>
  new Response(JSON.stringify(body), { status, headers: { ...corsHeaders, "Content-Type": "application/json" } });

Deno.serve(async (req) => {
  if (req.method === "OPTIONS") return new Response("ok", { headers: corsHeaders });

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL")!,
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!
  );

  const authHeader = req.headers.get("Authorization") ?? "";
  const { data: userData } = await supabase.auth.getUser(authHeader.replace("Bearer ", ""));
  const uid = userData?.user?.id;
  if (!uid) return json({ error: "UNAUTHENTICATED" }, 401);

  const { data: caller } = await supabase
    .from("digital_twins")
    .select("id, role, org_id")
    .eq("auth_user_id", uid)
    .maybeSingle();
  if (!caller || !["hr_executive", "hr_partner", "recruiter"].includes(caller.role)) {
    return json({ error: "FORBIDDEN", message: "Recruitment access required." }, 403);
  }

  let body: { document_id?: string } = {};
  try {
    body = await req.json();
  } catch {
    /* empty */
  }
  const documentId = (body.document_id ?? "").trim();
  if (!documentId) return json({ error: "VALIDATION_ERROR", message: "document_id is required." }, 400);

  const { data: doc } = await supabase
    .from("resume_documents")
    .select("id, org_id, twin_id, storage_path, file_name, content_type")
    .eq("id", documentId)
    .maybeSingle();
  if (!doc) return json({ error: "NOT_FOUND", message: "Resume document not found." }, 404);
  if (doc.org_id !== caller.org_id) return json({ error: "FORBIDDEN" }, 403);
  if (!doc.storage_path) return json({ error: "NOT_FOUND", message: "No original file is stored for this document." }, 404);

  // Expiring, authorized access (never a public URL).
  const { data, error } = await supabase.storage.from("resumes").createSignedUrl(doc.storage_path, 60);
  if (error || !data?.signedUrl) {
    return json({ error: "INTERNAL", message: "Could not create a download link." }, 500);
  }
  return json({ ok: true, url: data.signedUrl, file_name: doc.file_name, content_type: doc.content_type });
});
